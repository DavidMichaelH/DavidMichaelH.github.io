
---
layout: default
title: Home
---

# Welcome

This is my home page.


Gets rendered into a beautiful HTML page with headers, code formatting, math, etc.

---

## 🔢 **What About Math?**

If you want LaTeX-style math like `\int_0^\infty f(x) dx`, no problem:

- **Use MathJax** or **KaTeX** depending on your engine.
- Most static site generators support these with a config flag or plugin.

Example:
```markdown
Let $f(x)$ be a continuous function. Then:
$$
\int_{-\infty}^\infty f(x) dx = 1
$$

Gets rendered into a beautiful HTML page with headers, code formatting, math, etc.

---

## 🔢 **What About Math?**

If you want LaTeX-style math like `\int_0^\infty f(x) dx`, no problem:

- **Use MathJax** or **KaTeX** depending on your engine.
- Most static site generators support these with a config flag or plugin.

Example:
```markdown
Let $f(x)$ be a continuous function. Then:
$$
\int_{-\infty}^\infty f(x) dx = 1
$$
